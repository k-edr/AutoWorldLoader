namespace AutoWorldLoader
{
    /// <summary>
    /// Predefined world templates. Each value is a key into
    /// <see cref="WorldTemplateRegistry"/>.
    /// </summary>
    public enum WorldTemplate
    {
        /// <summary>Sentinel — no template selected.</summary>
        None,

        /// <summary>Empty world, no limits, programmable block on, no mods.</summary>
        EmptyWorld_NoLimits_WithPB_NoMods
    }
}
